---
okf_version: '0.2'
title: prompt-cache-linter knowledge bundle
description: 'Paste an Anthropic Messages API request with cache_control markers and see what is cached at each breakpoint, plus a flag on any dynamic content before a breakpoint that will silently break the cache. Runs entirely in the browser.'
---

# Product

* [prompt-cache-linter](product.md) - Paste an Anthropic Messages API request with cache_control markers and see what is cached at each breakpoint, plus a flag on any dynamic content before a breakpoint that will silently break the cache. Runs entirely in the browser.

# Subdirectories

* [playbooks](playbooks/index.md) - 1 Playbook.

# Tech Stack

* [prompt-cache-linter stack](stack.md) - Frameworks, storage and services prompt-cache-linter runs on.
